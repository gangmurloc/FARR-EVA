from __future__ import annotations

"""Compatibility module retained for serialized research artifacts.

The original workspace kept ``farr`` and ``farr_star`` in separate projects
and inserted a machine-specific path here.  The public package ships both
modules together, so no path mutation is required.
"""
